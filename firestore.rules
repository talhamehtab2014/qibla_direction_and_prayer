rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Applies to every document in your database
    match /{document=**} {
      allow read: if true;                    // anyone (public)
      allow write: if request.auth != null;   // only signed-in users
    }
  }
}
